from setuptools import setup  # type: ignore

setup(
    name="journal_parse",
    version="1.0",
    description="Journal Parser and Analysis Package",
    author="Joseh Palombo",
    packages=["journal_parse"],
    setup_requires=["numpy"],
)
